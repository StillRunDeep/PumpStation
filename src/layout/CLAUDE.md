**强制规约**：新增或修改功能时，必须先更新（或新建）对应模块的 `doc-*.md` 文档，再修改代码。
## 文档索引
**全局需求（`docs/`）**：开发前必读 `docs/requirements.md`。
**业务模型 (`model/`)**：房间定义与约束，见 `doc-泵站业务.md`。
**生成算法 (`generator/`)**：生长与优化，见 `doc-*.md`。
**拓扑规则 (`topology/`)**：邻接关系与走廊门禁计算。
**评价体系 (`evaluation/`)**：打分器，见 `doc-评价体系.md`。
**Revit 导出 (`export/`)**：序列化对接，见 `doc-Revit对接说明.md`。
 
