### 核心思路

基于\*\*网格（Grid）\*\*进行房间的放置与扩展，通过用户定义的约束条件（邻接关系、连通关系）控制生成结果，同时保证所有房间可达。

* * *

### 主要步骤

#### 第一步：层级布局划分（Hierarchical Layout）

将建筑按功能分区，形成层级结构。例如：

```
建筑
├── 公共区（Public Zone）→ 客厅、厨房、餐厅
└── 私人区（Private Zone）→ 走廊、卧室、浴室
```

同一层级内可定义邻接约束；跨层级的房间之间则通过"房间与父区域相邻"来间接关联。

* * *

#### 第二步：房间初始位置选取（Room Placement）

-   将建筑轮廓多边形**栅格化**为二维网格
-   建立**权重网格**：建筑内部格子权重为1，外部为0
-   根据房间估计面积，距外墙一定距离以内的格子权重设为1，其余设为0，以生成更规整的房间形状
-   根据**邻接约束**，将应相邻房间的周边格子权重调高，引导初始位置靠近
-   若邻接约束未被满足，则**重置生成过程**

* * *

#### 第三步：面积补偿与预处理（Area Compensation）

当用户定义的建筑轮廓远大于房间初始需求面积之和时，为了避免建筑内部出现大面积空置或不合理的走廊，算法引入了自动补偿机制：

1.  **目标利用率设定**：设定全局目标空间利用率（如 90%）。
2.  **分层计算**：独立计算每一层（地面层与一层）的房间需求总面积与当前建筑可用面积的比例。
3.  **等比缩放**：若该层总需求面积占比低于目标利用率，则自动计算补偿系数，将该层所有房间的“目标生长面积”等比放大。
4.  **动态适应**：确保生长后的房间能够更好地填充大型号的建筑轮廓，保持布局的紧凑性。

* * *

#### 第四步：房间扩展（Room Expansion）

扩展过程采用**严格三阶段**全局同步机制：

| 阶段 | 方法 | 说明 |
| --- | --- | --- |
| **阶段 1：全局矩形扩展** | `findBestRectangleExpansion` | **全局同步**：所有房间优先进行矩形生长。直到**没有任何房间**能继续以矩形方式扩展时，该阶段结束并记录“阶段 1”快照。 |
| **阶段 2：L/U 形填补扩展** | `findBestFillExpansion` & `findSmartLineExpansion` | **形态降级**：允许房间转为非矩形生长以填补剩余面积。**形态约束**：除走廊外，房间轮廓顶点数不得超过 **8 个**（最高支持 U 形），房间利用率（房间实际占据面积 / 房间包围盒面积）不得低于 **60%**（防止出现超大的拐角），在后期的评价指标中也会对低于 **70%** 的房间进行惩罚。 |
| **阶段 3：边界优化与空隙填充** | `fillGaps` & `Boundary-First Filling` | **缝隙收尾**：将建筑轮廓内剩余的所有空隙（Empty Cells）完全分配给相邻房间，并借此机会重新评估并平滑房间交界线，优化房间利用率，确保建筑内部无遗漏空间且轮廓方正。此阶段可能突破上述房间面积、形态约束。 |

每次选择哪个房间扩展，由其**面积完成率（currentArea / targetArea）**决定——比例越低的房间，被选中的优先级越高。


整个算法可运行 **N 次**，收集多个合法方案，再按评分（如走廊最小、转角最少）筛选最优解。

* * *

### 进化式批量生成策略（Evolutionary Batch Generation）

为了在保证方案多样性的同时不断进化出更优解，系统采用了“遗传变异 + 随机探索”的混合生成策略：

1. **初始阶段**：全随机生成 N 个方案。
2. **进化阶段（持续生成）**：
   - **精英保留与交叉（Hybrid）**：从现有方案池中选出精英，通过”种子位置交叉”产生后代，保留优质布局特征。
     - **配对规则**：第 1 名 ↔ 第 5 名、第 5 名 ↔ 第 9 名。通过分散亲本排名，防止由于第 1 名过度主导而导致种群基因单质化。
     - **变异**：后代种子位置取**亲本方案中该房间包围盒（Bounding Box）的中心点**（mm 坐标转换为网格坐标），而非亲本种子坐标的随机偏移。这样子代的起始位置与亲本实际布局对齐，保留空间结构的同时提供更强的探索能力。
   - **随机探索（Random）**：每轮保持一定比例的纯随机方案生成，用于打破局部最优，探索全新的空间组织可能性。
   - **配比**：每轮生成 9 个方案，其中 **2 个** 为交叉进化方案，**7 个** 为纯随机方案。
3. **筛选与实时排序**：每轮产生的新方案与旧方案合并，重新评分并保留全球前 9 名。

* * *

#### 第四步：连通性处理（Room Connectivity）

放置内部**门**的算法步骤：

1.  先为显式声明连通约束的房间放置门
2.  将走廊（Hallway）与所有相邻公共房间连通
3.  未连通的私人房间连接到相邻的公共房间
4.  无连接的公共房间连接到相邻公共房间
5.  最后做**可达性检验**：若有房间不可达，则沿邻接关系找路径并补充门

门的放置方式：在共享墙段上随机选取位置，朝目标房间方向开启。

* * *

#### 第五步：后处理（Post-processing）

-   生成内外墙体的几何表示
-   放置**外门与窗户**（按每个房间的需求声明）
-   多层建筑：底层生成楼梯间，上层复用相同位置，房间自然围绕楼梯扩展

* * *

### 算法特点与局限

**优点：** 速度快（简单平面 <100ms）、支持L形房间和非矩形建筑轮廓、约束灵活可定制

**局限：** 不支持任意角度建筑、门的位置随机导致走道不优化、无法约束单个房间的长宽比（如车库）

