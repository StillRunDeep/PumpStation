# AG3-1 设备流线二维示意

> 功能分类：**图形表达**
> 本体对象定义见 [00-计算定义总纲.md](./00-计算定义总纲.md)

---

## 一、职责概述

AG3-1 根据 AG0-1 输出的连接拓扑和 AG2-1 计算的空间尺寸规则，将所有设备按实际比例摆入维护间，并生成两个视图：

- **平面图（俯视）**：设备水平布局、管件（弯头/止回阀/闸阀/流量计）按实际比例绘制
- **A-A 剖面图**：竖向空间关系，三水位标注，切割线可拖动

---

## 二、在系统中的位置

```
AG0-1（连接关系配置器）  → ConnectionTopology
AG1-2（水泵选型）        → P_motor, catalogPump（含 dimensions_mm）
AG1-3（管道尺寸计算）    → DN_pumpIn, DN_pumpOut, DN_mainOutlet
AG1-1（污水池计算）      → h_wetWell, stopLevel, startLevel, alarmLevel
AG2-1（维护间尺寸）      → L, W, hasCatalogDims, c_wall_m, L_elbow_m, DN_branch, DN_main
AG0-0（用户对话）        → N
  ▼
AG3-1（设备流线二维示意）
  │  输出：SVG 平面图 + A-A 剖面图
  ▼
网页展示区域
```

---

## 三、输入参数

| 参数名 | 来源 | 单位（SI） | 说明 |
|--------|------|-----------|------|
| ConnectionTopology | AG0-1 | — | 节点 + 边，含所有设备和管道连接关系 |
| P_motor | AG1-2 | kW | 用于判断间距规则 |
| catalogPump | AG1-2 | — | 可选，泵目录匹配结果（含 `dimensions_mm`）|
| DN_pumpIn | AG1-3 | mm | 吸水管管径（绘图参考宽度） |
| DN_pumpOut | AG1-3 | mm | 压水管管径（= DN_branch） |
| DN_mainOutlet | AG1-3 | mm | 总出水管管径（= DN_main） |
| h_wetWell | AG1-1 | m | 污水池总深度（剖面图）|
| stopLevel | AG1-1 | m | 停泵水位（剖面图标注）|
| startLevel | AG1-1 | m | 启泵水位（剖面图标注）|
| alarmLevel | AG1-1 | m | 报警水位（剖面图标注）|
| N | AG0-0 | 台 | 工作泵台数 |
| N_spare | AG0-0（高级参数） | 台 | 备用泵台数，默认 0；AG12 以 N_total=N+N_spare 计算泵房长度 |
| L | AG2-1 | m | 维护间净长 |
| W | AG2-1 | m | 维护间净宽 |
| hasCatalogDims | AG2-1 | bool | 是否使用 catalog 实际尺寸 |
| c_wall_m | AG2-1 | m | 管中心离集水坑墙距离 |
| L_elbow_m | AG2-1 | m | 弯头水平投影长度 |

---

## 四、维护间尺寸计算

> 见 AG2-1 规格文档。

---

## 五、绘制内容

### 5.1 坐标系

| 视图 | X 轴 | Y 轴 | 说明 |
|------|------|------|------|
| 平面图 | 向右（泵排列方向）| 向下（通道方向）| 俯视 |
| A-A 剖面图 | 室内净宽 W | 向上（高程）| 竖向 |

两视图并列于同一 SVG 画布；剖面切线在平面图中以虚线及"A"符号标注。

### 5.2 平面图绘制对象

| 对象 | 绘制方式 | 位置依据 |
|------|----------|----------|
| 维护间外轮廓 | 矩形，L_room × W_room | 全局边界 |
| 污水池 | 矩形，面积 = S（或 S_min），以维护间为中心对称 | 维护间进水侧 |
| 水泵机组 × N_total | 矩形块，`w_pump × d_pump`（catalog 实际尺寸或默认值 0.6×0.8 m）| 一字排开，按 d_spacing 和 e_wall 定位 |
| 管件（按实际比例）| `_elbow`（弧形弯头）、`_checkValve`（菱形）、`_gateValve`（矩形）、`_flowmeter`（流量计）、`_tee`（T型节点）、`_reducer`（梯形大小头） | 基于 `c_wall_m`、`L_elbow_m`、阀件面对面长度定位 |
| MainOutlet 干管 | 横向粗线 | 紧靠维护间背墙（hdr_y） |
| 控制柜 | 矩形块 | 维护间端部角落 |
| 剖面切线 A-A | 水平虚线 + "A"标注 + 可拖动 | 初始穿过泵排中心，可鼠标拖动 |

**管件绘制细节（平面图）：**

每台泵的支管从泵出口向下到穿墙洞口，再水平弯头转向总管方向，依次经过：
1. 穿墙洞口（圆形表示）
2. 90° 弯头（弧形，1.5×DN）
3. 水平直管段（阀件间直管段，取 max(2D, 300) mm）
4. 止回阀（菱形符号，GB/T 12221 面对面长度）
5. 水平直管段
6. 闸阀（矩形符号，GB/T 12221 面对面长度）
7. 连接管（竖向）到总管（T 型节点）
8. 总管（横向粗线）
9. 总管闸阀（矩形）
10. 电磁流量计（矩形+圆形叠加，含 5D/2D 直管段标注）

若某段长度太短（<15px）无法放下尺寸文字，使用 `_leader` 引线引出标注。

### 5.3 A-A 剖面图绘制对象

泵房净高估算：

```
room_H = max(3.0, h_wetWell × 0.2 + 1.0)    （m）
```

| 要素 | 表达方式 | 说明 |
|------|----------|------|
| 室外地坪线（±0.00）| 粗实线 | 地上/地下分界 |
| 泵房围护结构 | 矩形，W_room × room_H | 地上部分 |
| 污水池池壁 | 矩形，W_room × h_wetWell | 地下部分 |
| 水面示意 | 蓝色填充 | 从池底到报警水位 |
| 停泵水位 stopLevel | 绿色水平虚线 + 标注 | 距池底 |
| 启泵水位 startLevel | 橙色水平虚线 + 标注 | 距池底 |
| 报警水位 alarmLevel | 红色水平虚线 + 标注 | 距池底 |
| 水泵机组（剖面）| 矩形 | 泵房内地坪处 |
| 吸水管（PumpInlet）| 竖向线段向下 | 穿底板进入污水池 |
| 压水管（PumpOutlet）| 竖向线段向上 | 水泵出口引出 |
| 竖向尺寸标注 | 双向箭头 + 文字 | room_H, h_wetWell |
| 水平尺寸标注 | 双向箭头 + 文字 | W_room |

**A-A 切割线可拖动：**

切割线 `<line id="aa-drag-line">` 初始位于泵排中心（Y = pump_row_y），约束在 `room_oy ~ room_y2` 范围内拖动。

拖动时记录 `aaDragY`（module 变量），同步更新 `<line>` 位置 + 重绘剖面内容。

剖面内容按切割线位置切换（沿 W 方向分为三段）：

| 切割线位置（相对 W）| 剖面显示 |
|---------------------|----------|
| 0 ~ 25%（靠集水坑墙）| 泵出口竖管椭圆截面 + 弯头 + 大小头，含 DN 标注 |
| 25% ~ 75%（中间管道区）| 支路管道圆截面 + 止回阀/闸阀符号，含 DN 标注 |
| 75% ~ 100%（靠远侧墙）| 总管圆截面 + 流量计（含 5D/2D 上游/下游标注）|

每段管道圆截面半径 = DN/1000 × ss（含 DN 标注文字）。

### 5.4 剖面组拖动

剖面图所有元素包裹在 `<g id="section-panel" transform="translate(dx,dy)">` 内，`mousedown/mousemove/mouseup` 事件拖动该 transform（用 `stopPropagation` 避免触发整体 SVG 平移）。

reset 按钮回调时重置 dx=dy=0。

### 5.5 最小缩放保护

SVG zoom/pan 限制最小缩放比例：

```
minScale: 1.0    // 初始充满屏幕，不得缩小
maxScale: 6      // 防止过大导致看不到全貌
```

调用 `initSvgZoomPan(el, VW, VH, btnIds, { minScale: 1.0, maxScale: 6, onReset })`。

### 5.6 缩放比例

```
scale_plan = min(
  plan_area_width  / L_pool,
  plan_area_height / (W_room + D_pool)
)    （px/m）

scale_section = min(
  section_area_width  / (W_room + 1.5),
  section_area_height / (room_H + h_wetWell + 0.5)
)    （px/m）
```

---

## 六、引用规则

| 规则 ID | 简述 |
|---------|------|
| R-LA-01 | 相邻水泵净距 ≥ 1.0 m（大型泵 ≥ 1.2 m）|
| R-LA-02 | 泵距侧墙 ≥ 0.8 m（大型泵 ≥ 1.0 m）|
| R-LA-03 | 大型泵（P_motor > 55 kW）加大间距 |
| R-LA-04 | 每台泵独立吸水管（PumpInlet）|
| R-LA-08 | 就地控制柜布置在角落 |
| R-CM-03 | 停泵水位高于水泵进口 ≥ 0.5 m（剖面图可视验证）|
| R-CM-04 | 水位顺序：stopLevel < startLevel < alarmLevel |
| R-OP-01 | 同型号水泵，w_pump 相同 |
| R-OP-03 | 备用泵同规格，数量 N_spare（可为零） |
| R-OP-05 | 水泵左右对称布置 |
| GB/T 12221 | 闸阀/止回阀面对面尺寸 |
| GB/T 12459 | 90° 弯头中心到端面 = 1.5×DN |
| GB 50265-2022 §7 | 管外壁到墙面净距 ≥ 800 mm |

---

## 七、输出

```json
{
  "L_room": "<维护间净长，m>",
  "W_room": "<维护间净宽，m>",
  "d_spacing": "<实际泵间净距，m>",
  "e_wall": "<端部距墙净距，m>",
  "svg": "<SVG 元素，嵌入网页展示>"
}
```

SVG 包含：
- 左侧平面图（俯视，设备 + 拓扑管道 + 管件实际比例符号）
- 右侧 A-A 剖面图（竖向空间 + 三水位标注 + 切割线可拖动 + 管道圆截面按位置变化）
- 工具栏（放大/缩小/重置）带最小缩放保护