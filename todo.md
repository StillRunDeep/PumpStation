在本文中持续记录排查的思路和进展，不超过500字。

目标：解决生成迭代无法进一步优化方案的问题。

背景：
- 整体的算法流程：：
    - 文档：doc-泵站业务.md内章节6.4 算法流程（每次随机初始化）
    - 实现：agents/layout-build.js，
- 遇到问题：
    - 多方案生成效果不佳，无法走通 6.4中的检查点A
- 已确认的根本假设：
    - 重新放置的位置不合理（种子"落点质量"问题，权重更高）；
- 可视化辅助debug设置：
    - 点击优化方案时，在调试视图中，用箭头标记房间种子的移动尝试；
    - _bypassCheckpointA=true：只调用computeMutatedLayout，持续点击优化方案
        - 有时异常：会产生新布局（已有return，原因待查，先加log确认路径）；
    - _bypassCheckpointA=false：只调用generateMutatedLayout，没有产生更好的方案；

---

## 任务计划

### Task 1：提升种子落点质量（核心假设验证）
重构优化方案的算法
- [x] 根因确认：原邻接权重提升区域在 Partner 中心点周围，因“落点防重叠”机制，高权重区被判定为已占用，导致权重图失效退化为随机放置。
- [] 修改方向：将权重计算从“基于中心点”改为“基于 Partner 真实轮廓向外发光”，避开占位区。
- []  把"合格房间的种子"固定到放置结果中心
- []  把“不合格房间的种子”摆放到有约束关系的房间的边缘

### Task 2（后续，暂不启动）
- LUgrow
- FillandAdjust
- 正式视图的拟合问题
